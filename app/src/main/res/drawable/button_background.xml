<?xml version="1.0" encoding="utf-8"?>
<shape xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- Hintergrundfarbe -->
    <solid android:color="@color/phileBeige" />

    <!-- Abgerundete Ecken -->
    <corners android:radius="12dp" />

    <!-- Rand (Stroke) -->
    <stroke
        android:width="2dp"
        android:color="@android:color/black" />
</shape>
