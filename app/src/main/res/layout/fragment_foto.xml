<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@color/phileGreen"
    android:id="@+id/foto_main"
    android:orientation="vertical"
    tools:context=".FotoFragment">

    <!-- Textblock -->
    <TextView
        android:id="@+id/text_block"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="16dp"
        android:background="@drawable/rounded_background"
        android:padding="16dp"
        android:text="@string/fototext"
        android:textColor="@android:color/black"
        android:textSize="16sp" />

    <!-- Bereich mit zwei Iconbuttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_margin="20dp"
        android:background="@drawable/rounded_background"
        android:gravity="center"
        android:padding="20dp"
        android:orientation="horizontal">

        <!-- Kamera Icon Button -->
        <ImageButton
            android:id="@+id/button_camera"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_marginEnd="30dp"
            android:background="@drawable/button_background"
            android:padding="10dp"
            android:scaleType="centerInside"
            android:src="@drawable/camera"
            android:contentDescription="Foto machen" />

        <!-- Galerie Icon Button -->
        <ImageButton
            android:id="@+id/button_gallery"
            android:layout_width="60dp"
            android:layout_height="60dp"
            android:layout_marginStart="30dp"
            android:background="@drawable/button_background"
            android:padding="10dp"
            android:scaleType="centerInside"
            android:src="@drawable/gallery"
            android:contentDescription="Galerie öffnen" />
    </LinearLayout>

</LinearLayout>
